# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

# User specific aliases and functions

PS1="\[\e[01;31m\]\u@SEC1-PROD \`pwd\` \[\e[m\]\$ "

export PATH=/home/levelup/script/productionscript:$PATH
export PYTHONPATH=/home/levelup/script/python:$PYTHONPATH
export STATICDATAPATH=/home/levelup/static_data

